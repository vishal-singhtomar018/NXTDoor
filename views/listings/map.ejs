<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Tourism Map</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
    <style>
        #map {
            height: 600px;
            width: 100%;
        }
    </style>
</head>

<body>
    <h2>Explore Places on Map</h2>
    <div id="map"></div>

    <script>
        // Initialize map
        const map = L.map('map').setView([20.5937, 78.9629], 5); // India center as default

        // Add OpenStreetMap tiles
        L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
            maxZoom: 19,
            attribution: '© OpenStreetMap contributors'
        }).addTo(map);

        // DB data passed from backend
        const listings = <% - JSON.stringify(listings) %>;

        listings.forEach(place => {
            if (place.latitude && place.longitude) {
                L.marker([place.latitude, place.longitude])
                    .addTo(map)
                    .bindPopup(`<b>${place.title}</b><br>${place.location}<br>₹${place.price}`);
            }
        });
    </script>
</body>

</html> -->